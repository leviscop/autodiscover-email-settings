<?xml version="1.0" encoding="utf-8" ?>
<Autodiscover xmlns="http://schemas.microsoft.com/exchange/autodiscover/responseschema/2006">
	<Response xmlns="{{schema}}">
		<User>
			<DisplayName>{{info.name}} Email</DisplayName>
		</User>

		{%- if imaps.host %}
		<Account>
			<AccountType>email</AccountType>
			<Action>settings</Action>
			<ServiceHome>{{info.url}}</ServiceHome>

			<Protocol>
				<Type>IMAP</Type>
				<TTL>1</TTL>

				<Server>{{imaps.host}}</Server>
				<Port>{{imaps.port}}</Port>

				<LoginName>{{email}}</LoginName>

				<DomainRequired>on</DomainRequired>
				<DomainName>{{domain}}</DomainName>

				<SPA>off</SPA>
				<Encryption>{{imapsenc}}</Encryption>
				<AuthRequired>on</AuthRequired>
			</Protocol>
		</Account>
		{% endif -%}

		{%- if pops.host %}
		<Account>
			<AccountType>email</AccountType>
			<Action>settings</Action>
			<ServiceHome>{{info.url}}</ServiceHome>

			<Protocol>
				<Type>POP</Type>
				<TTL>1</TTL>

				<Server>{{pops.host}}</Server>
				<Port>{{pops.port}}</Port>

				<LoginName>{{email}}</LoginName>

				<DomainRequired>on</DomainRequired>
				<DomainName>{{domain}}</DomainName>

				<SPA>off</SPA>
				<Encryption>{{popsenc}}</Encryption>
				<AuthRequired>on</AuthRequired>
			</Protocol>
		</Account>
		{% endif -%}

		{%- if smtps.host %}
		<Account>
			<AccountType>email</AccountType>
			<Action>settings</Action>
			<ServiceHome>{{info.url}}</ServiceHome>

			<Protocol>
				<Type>SMTP</Type>
				<TTL>1</TTL>

				<Server>{{smtps.host}}</Server>
				<Port>{{smtps.port}}</Port>

				<LoginName>{{email}}</LoginName>

				<DomainRequired>on</DomainRequired>
				<DomainName>{{domain}}</DomainName>

				<SPA>off</SPA>
				<Encryption>{{smtpsenc}}</Encryption>
				<AuthRequired>on</AuthRequired>
			</Protocol>
		</Account>
		{% endif -%}

		{%- if mobilesync.url %}
		<Action>
			<Settings>
				<Server>
					<Type>MobileSync</Type>
					<Url>{{mobilesync.url}}</Url>
					{%- if mobilesync.name %}
					<Name>{{mobilesync.name}}</Name>
					{% endif -%}
				</Server>
			</Settings>
		</Action>
		{% endif -%}
	</Response>
</Autodiscover>
